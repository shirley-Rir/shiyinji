# 拾音记：Web MVP PRD

## 1. 版本定位

本文档定义拾音记 Web MVP 的第一版产品需求。它不是最终商业版说明，也不是硬件方案说明，而是用于指导首轮 Web 原型开发、验收和小规模验证。

本版 MVP 的目标是验证：用户通过文本或图片表达情境后，系统结合账号级音乐画像给出的 Top 1 歌曲，是否足够贴合当下，并能在网页内真实完整播放。

## 2. MVP 目标

### 2.1 产品目标

- 让用户无需搜索歌名或歌单，也能用一句话或一张图片发起听歌。
- 系统能把用户输入解析成可用于推荐的结构化情境。
- 推荐结果默认播放 Top 1，并展示 4 首可切换备选。
- 播放、跳过、喜欢、不喜欢和方向调整会写入账号画像。
- 支持首轮验证情绪陪伴、学习/工作、旅游三类场景。

### 2.2 验证目标

- 验证“情境输入”是否比普通歌单选择更省力。
- 验证“默认播放 Top 1”是否比先展示列表更接近音乐消费习惯。
- 验证账号级画像是否能让后续推荐变准。
- 验证文本、图片、图文组合三种输入在不同场景下的效果差异。

### 2.3 非目标

- 不做硬件形态、摄像头主动识别和设备端交互。
- 不做公开商业化发布。
- 不做社交、评论、歌单社区。
- 不做歌词、均衡器、无损音质、跨设备续播。
- 不绕过会员、地区、版权下架或平台技术限制。
- 不做心理诊断或治疗建议。

## 3. 目标用户与测试范围

### 3.1 目标用户

首轮用户是经常听歌、但在某些时刻不知道该听什么的人。他们可能在以下场景中使用：

- 情绪陪伴：低落、疲惫、焦虑、想被安静陪着。
- 学习/工作：专注、写作、阅读、整理材料。
- 旅游途中：看风景、坐车、散步、行程结束后的放松。

### 3.2 测试范围

第一轮建议为 20 名左右明确知情的受邀用户，每人至少完成 5 次不同情境会话。若音源仍依赖第三方网易云 API，则测试必须保持封闭，不做互联网公开招募。

## 4. 用户旅程

### 4.1 首次进入

1. 用户注册或登录拾音记账号。
2. 用户完成最小冷启动偏好，所有问题可跳过。
3. 用户选择是否连接网易云音乐账号，用于导入歌单偏好和验证播放权限。
4. 用户进入主播放界面。

### 4.2 发起一次情境播放

1. 用户输入一句话、上传图片，或同时提供图文。
2. 用户点击“开始听”。
3. 系统解析情境，生成统一的 `ContextSession`。
4. 系统召回当前用户可播放的候选歌曲。
5. 系统排序并返回 Top 1、4 首备选和后续队列。
6. 页面开始播放 Top 1。
7. 用户继续听、切换备选、换一首或反馈。
8. 系统记录行为并更新画像。

### 4.3 再次使用

用户再次表达情境时，系统应利用此前的显式偏好、历史反馈和相似场景行为进行重排。用户应感觉系统“更懂这个账号”，而不是每次都像第一次随机推荐。

## 5. 功能需求

### 5.1 账号系统

**优先级：P0**

需求：

- 支持注册、登录、退出。
- 画像、会话、反馈和历史记录绑定拾音记账号。
- 支持查看和删除主要历史数据。
- 支持关闭个性化记录。关闭后仍可使用单次情境推荐，但不跨会话学习。

验收：

- 同一账号在不同浏览器登录后能看到同一画像与历史。
- 用户删除历史后，后续推荐不再引用已删除会话。

### 5.2 网易云连接

**优先级：P0，但仅用于封闭原型**

需求：

- 支持用户本人授权网易云会话。
- 优先扫码或 Cookie 会话，不接收网易云明文密码。
- 能导入用户歌单、收藏和基础偏好信息。
- 能校验歌曲是否对当前用户可完整播放。
- 支持解绑网易云连接，并清除会话凭据。

边界：

- 不启用解灰、代理、音源替换、随机 IP 或其他绕过能力。
- 不缓存、下载、转存或分发完整音频。
- 第三方 API 不可用时应降级，而不是规避限制。

验收：

- 成功授权后能读取歌单和部分曲目元数据。
- 授权失效后不能继续调用凭据播放。
- 解绑后本地保存的网易云会话不可再用。

### 5.3 冷启动偏好

**优先级：P0**

需求：

- 收集喜欢的歌手、歌曲、风格、语种。
- 收集明确不喜欢的歌手、风格、语种。
- 收集“熟悉歌曲 vs 发现新歌”的倾向。
- 所有问题可跳过。

验收：

- 用户 2 分钟内可完成或跳过初始化。
- 跳过后仍可进入推荐，不因画像不足阻塞播放。

### 5.4 情境输入

**优先级：P0**

需求：

- 支持自然语言输入。
- 支持用户主动上传图片。
- 支持图文组合输入。
- 文本和图片都输出同一个结构化情境对象。

结构化情境至少包含：

```json
{
  "input_type": "text_image",
  "mood": ["疲惫", "平静"],
  "activity": "旅行后休息",
  "environment": ["海边", "傍晚", "开阔"],
  "intent": "放松并停留在此刻",
  "energy_target": 30,
  "lyric_density": "medium",
  "familiarity_bias": "familiar",
  "constraints": ["不要太吵"],
  "transition": "从疲惫到舒展",
  "confidence": 0.82
}
```

验收：

- 纯文本、纯图片、图文组合均可生成结构化情境。
- 低置信度时只问一个简短问题完成消歧。
- 默认不长期保存原始图片，只保存情境标签和处理结果。

### 5.5 推荐与排序

**优先级：P0**

需求：

- 推荐必须基于真实曲库 ID。
- 推荐前必须过滤当前不可播放歌曲。
- 排序同时考虑本次情境、长期画像、场景偏好和明确约束。
- Top 1 以稳定命中为目标，备选承担探索和方向差异。

验收：

- 返回 1 首默认播放歌曲、4 首备选、后续播放队列。
- 每首推荐都有简短理由，但成功指标以播放和反馈行为为准。
- 同一情境下连续推荐不应全部来自同一歌手或同一风格。

### 5.6 播放器

**优先级：P0**

需求：

- 支持播放、暂停、上一首、下一首。
- 支持进度条、拖动定位、音量控制。
- 展示歌名、歌手、封面、当前队列。
- 支持当前歌曲结束后继续播放队列。
- 展示加载、播放失败、授权失效、不可用状态。

验收：

- 在音源可用条件下，用户点击“开始听”后 8 秒内开始播放。
- 切换备选后，当前歌曲立即替换，并记录选择行为。
- 播放失败时用户能继续尝试下一首可播放候选。

### 5.7 反馈与画像学习

**优先级：P0**

需求：

- 支持喜欢、不喜欢、换一首、多来点这种。
- 支持更安静、更有劲、更熟悉、更新鲜等方向调整。
- 区分“本次情境不适合”和“长期不喜欢”。
- 支持撤销最近一次反馈。

验收：

- 每次反馈都能追溯到账号、歌曲、情境、推荐位置和时间。
- 明确不喜欢的歌手或风格会影响后续排序。
- 单次换歌不应永久拉黑该歌曲。

### 5.8 历史与画像管理

**优先级：P0**

需求：

- 用户可查看最近会话、主要偏好和系统学习到的场景偏好。
- 用户可删除会话记录。
- 用户可修正显式偏好。
- 用户可关闭个性化学习。

验收：

- 删除和关闭操作在界面上可见、可执行、可恢复使用。
- 关闭个性化后，系统不再写入跨会话画像。

### 5.9 数据与隐私提示

**优先级：P0**

需求：

- 首次使用时说明会保存哪些数据。
- 图片默认只短暂处理，不长期保存原图。
- 网易云连接与拾音记账号明确区分。
- 用户可解绑第三方音乐账号。

验收：

- 用户能在设置中看到个性化开关、历史删除、网易云解绑入口。
- 后端不保存网易云明文密码。

## 6. 页面范围

Web MVP 至少包含以下页面或视图：

- 登录/注册页。
- 冷启动偏好页。
- 网易云连接页。
- 主播放页。
- 推荐备选与队列视图。
- 历史与画像页。
- 设置与隐私页。

主播放页是产品第一屏，不做营销落地页。用户登录后应直接进入可输入情境和播放音乐的界面。

## 7. 状态与异常

必须处理以下状态：

- 未登录。
- 未连接音乐账号。
- 音乐账号授权失效。
- 输入为空。
- 情境理解低置信度。
- 图片上传失败。
- 推荐失败。
- 无可播放候选。
- 播放失败。
- 网络中断。
- 用户关闭个性化。

自伤、自杀等高风险表达出现时，应优先展示即时支持信息，音乐只能作为附加选择。该能力不把产品包装成心理健康服务。

## 8. 验收指标

第一轮建议指标：

| 指标 | 目标 |
| --- | --- |
| 首曲播放启动成功率 | 90% 以上 |
| 提交到出声耗时 | P75 小于 8 秒 |
| Top 1 接受率 | 55% 以上 |
| 前三首有效接受率 | 75% 以上 |
| 用户重复使用率 | 40% 以上 |
| 情境匹配“符合”占比 | 60% 以上 |
| 用户能说清产品差异 | 70% 以上 |

Top 1 接受率暂定义为：首曲开始后 30 秒内未被主动跳过，或用户明确点击喜欢。测试报告需同时记录实际播放时长，避免误判。

## 9. 交付物

第一版 Web MVP 需要交付：

- 可运行 Web 应用。
- 用户账号、画像、推荐、播放和反馈闭环。
- `MusicProvider` 适配层。
- 文本和图片情境解析能力。
- 基础事件埋点和验证看板数据。
- 小规模测试记录模板。
- 音源合规边界说明。

## 10. 发布门槛

进入受邀测试前必须满足：

- 本地或封闭环境内完成完整播放验证。
- 不可播放、授权失效、推荐失败均有降级状态。
- 图片默认不长期保存原图。
- 网易云凭据加密保存且可解绑删除。
- 关闭解灰、代理、音源替换、随机 IP 等绕过能力。
- 有明确测试说明，让参与者知道这是非公开原型。



